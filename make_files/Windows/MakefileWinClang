# Compatibility wrapper. The maintained build is the root Makefile.
.DEFAULT_GOAL := all
GLVM_ROOT := $(abspath $(dir $(lastword $(MAKEFILE_LIST)))/../..)
.PHONY: all $(MAKECMDGOALS)
all $(filter-out all,$(MAKECMDGOALS)):
	$(MAKE) -C "$(GLVM_ROOT)" -f Makefile PLATFORM=windows $@
